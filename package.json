{
  "name": "netology-cypress-1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "eslint": "eslint",
    "firefox": "npx cypress run --config-file cypress.json --browser firefox",
    "firefox-headed": "npx cypress run --config-file cypress.json --browser firefox --headed",
    "chrome-mobile": "npx cypress run --config-file cypress.env.json --browser chrome",
    "chrome-mobile-headed": "npx cypress run --config-file cypress.env.json --browser chrome --headed"
  },
  "author": "Mikhail Ponomarev",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.5.4"
  },
  "devDependencies": {
    "eslint": "^8.12.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.25.4",
    "eslint-plugin-prettier": "^4.0.0"
  }
}
